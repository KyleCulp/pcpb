<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "part-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= label f, :name %>
  <%= text_input f, :name %>
  <%= error_tag f, :name %>

  <%= label f, :partid %>
  <%= number_input f, :partid %>
  <%= error_tag f, :partid %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
